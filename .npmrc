# This project lives on an exFAT drive (no symlink support), so npm cannot create
# the node_modules/.bin/* symlinks. Disabling bin-links lets a plain `npm i` work.
# Run package binaries via `npx` (e.g. `npx next dev -p 3001`) since `.bin/` is empty.
bin-links=false
